# FLASK NGINX in my container.

semua app flask ada di dalam direktory app.

caranya runningnya.

dari direktori ini.
    docker build -t flaskapp .

lalu jalankan:
    docker run -p 80:80 flaskapp




